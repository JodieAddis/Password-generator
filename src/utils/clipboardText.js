/**
 *
 * @param {String} password
 * @param {String} copyPassword
 */
/*export const clipboardText = (password, copyPassword) => {
  navigator.clipboard.writeText(password);

  copyPassword.innerHTML = password;
};*/
